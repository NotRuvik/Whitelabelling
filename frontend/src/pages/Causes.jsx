import React from 'react';
import { Typography, Paper } from '@mui/material';

const Causes = () => {
    return (
        <Paper sx={{p: 3}}>
            <Typography variant="h4">View Causes</Typography>
            <Typography>A history of all Causes will be displayed here.</Typography>
        </Paper>
    );
}

export default Causes;